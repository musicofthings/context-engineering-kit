{
  "_comment": "API documentation sources — refreshed by the session-start hook when api_docs.md is older than refresh_ttl_hours, and weekly by GitHub Actions as a shared fallback",
  "_usage": "Edit to add/remove APIs. Sections are extracted keywords from the docs. A project-local config/api_sources.json overrides the kit's copy, so each project can pin its own stack's docs.",

  "refresh_ttl_hours": 24,

  "apis": [
    {
      "name": "anthropic-claude-code",
      "description": "Claude Code hooks, skills, slash commands, subagents",
      "base_url": "https://docs.anthropic.com/en/docs/claude-code/hooks",
      "extract_sections": ["Hook events", "SessionStart", "PreCompact", "Stop", "SessionEnd"],
      "update_frequency": "weekly",
      "cache_ttl_hours": 168
    },
    {
      "name": "anthropic-api",
      "description": "Claude API messages, models, pricing",
      "base_url": "https://docs.anthropic.com/en/api/getting-started",
      "extract_sections": ["models", "rate limits", "pricing", "messages"],
      "update_frequency": "weekly",
      "cache_ttl_hours": 168
    },
    {
      "name": "cloudflare-workers",
      "description": "Cloudflare Workers API for edge deployment",
      "base_url": "https://developers.cloudflare.com/workers/",
      "extract_sections": ["Workers", "KV", "R2", "Durable Objects", "Pages"],
      "update_frequency": "weekly",
      "cache_ttl_hours": 168
    }
  ],

  "_add_more": [
    "Copy one of the above blocks and adjust name/base_url/extract_sections",
    "Common additions: GitHub API, PubMed E-utilities, gnomAD GraphQL, ClinVar API"
  ]
}
